{% extends 'base.html.twig' %}

{% block title %}Report{% endblock %}

{% block body %}
    <h1>Report</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ report.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ report.title }}</td>
            </tr>
            <tr>
                <th>MeetingDate</th>
                <td>{{ report.meetingDate ? report.meetingDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Link</th>
                <td>{{ report.link }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('report_index') }}">back to list</a>

    <a href="{{ path('report_edit', {'id': report.id}) }}">edit</a>

    {{ include('admin_report/_delete_form.html.twig') }}
{% endblock %}
