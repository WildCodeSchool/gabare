{% extends 'layoutAdmin.html.twig' %}

{% block title %}Editer l'Alerte{% endblock %}

{% block body %}
    <div class="container-fluid p-5">
        <h1 class="ml-3 mb-4">Editer l'alerte</h1>
        <div class="col-lg-8 m-0 jumbotron">
            {{ form_start(form) }}
            {{ form_row(form.message) }}
            {{ form_row(form.activated) }}
            <button class="btn btn-primary mt-2">Mettre à jour</button>
            {{ form_end(form) }}
        </div>
        <div class="row mt-5 ml-1">
            <a href="{{ path('alert_index') }}" class="btn btn-outline-dark mr-5">Retour</a>
        </div>
    </div>

{% endblock %}
