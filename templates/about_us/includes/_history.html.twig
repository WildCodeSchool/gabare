<section class="history_section">
    <div class="container">
        {% if history is defined %}
            {% for historic in history %}
                <div class="row">
                    <div class="col-md-4">
                        <h2 class="mt-md-4 mx-auto title_history">{{ historic.title }}</h2>
                        <div class="gabare_image">
                            <img src="{{ historic.image }}" alt="Image de La Gabare"/>
                        </div>
                    </div>
                    <div class="col-md-8">
                        <p>{{ historic.description|raw }}</p>
                        <div class="button_join_us_div">
                            <button class="button_join_us">Nous rejoindre</button>
                        </div>
                    </div>
                </div>
            {% endfor %}
        {% else %}
            <p>Rien à afficher</p>
        {% endif %}
    </div>
</section>
