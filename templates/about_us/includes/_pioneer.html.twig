<section class="bgPioneer links" id="les-pionniers">
    <h2 class="text-center mt-md-5 mb-3 title-animation">Les pionniers</h2>
    <div class="container-fluid">
        <div class="row justify-content-center m-0 m-md-5">
                {% for pioneer in pioneers %}
                <p class="col-4 col-md-2">{{ pioneer.firstname }} {{ pioneer.lastname }}</p>
                {% endfor %}
        </div>
    </div>
</section>
