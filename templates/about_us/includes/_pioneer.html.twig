<section class="bgPioneer">
    <h2 class="text-center mt-md-5 mb-3 title-animation">Les pionniers</h2>
    <div class="container-fluid">
        <div class="row justify-content-center justify-content-md-between m-3 m-md-5">
                {% for pioneer in pioneers %}
                <p class="m-1">{{ pioneer.firstname }} {{ pioneer.lastname }}</p>
                {% endfor %}
        </div>
    </div>
</section>
