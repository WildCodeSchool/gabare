{% extends 'base.html.twig' %}

{% block title %}Presse{% endblock %}

{% block body %}
    <h1>Presse</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ presse.id }}</td>
            </tr>
            <tr>
                <th>Video</th>
                <td>{{ presse.video }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ presse.image }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ presse.title }}</td>
            </tr>
            <tr>
                <th>Resume</th>
                <td>{{ presse.resume }}</td>
            </tr>
            <tr>
                <th>Link</th>
                <td>{{ presse.link }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('presse_index') }}">back to list</a>

    <a href="{{ path('presse_edit', {'id': presse.id}) }}">edit</a>

    {{ include('presse/_delete_form.html.twig') }}
{% endblock %}
