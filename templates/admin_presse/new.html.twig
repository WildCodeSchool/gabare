{% extends 'layoutAdmin.html.twig' %}

{% block title %}Ajouter une revue de Presse{% endblock %}

{% block body %}
    <div class="container-fluid p-5">
        <h1>Ajouter une revue de presse</h1>
        <div class="col-lg-8 pt-3 pb-3 m-0 jumbotron">
            {{ form_start(form) }}
            {{ form_row(form.video) }}
            {{ form_row(form.image) }}
            {{ form_row(form.title) }}
             {{ form_row(form.resume) }}
            {{ form_row(form.link) }}
            <button class="btn btn-primary mt-2">Ajouter</button>
            {{ form_end(form) }}

    <div class="mt-4">
        <a href="{{ path('presse_index') }}" class="btn btn-outline-dark">Retour à la liste</a>
    </div>
    </div>

    </div>
{% endblock %}
{% block javascripts %}
    {{ encore_entry_script_tags('vich') }}
{% endblock %}
