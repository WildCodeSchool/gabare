{% extends 'layoutAdmin.html.twig' %}

{% block title %}Editer le contenu{% endblock %}

{% block body %}
    <div class="container-fluid p-5">
        <h1>Editer le contenu</h1>
        <div class="col-lg-8 m-0 jumbotron">
            {{ form_start(form) }}
            {{ form_row(form.video) }}
            {{ form_row(form.image) }}
            {{ form_row(form.title) }}
            {{ form_row(form.resume) }}
            {{ form_row(form.link) }}
            <button class="btn btn-primary mt-2">Mettre à jour</button>
            {{ form_end(form) }}
        </div>
        <div class="row mt-5 ml-1">
            <a href="{{ path('presse_show', {'id': presse.id}) }}" class="btn btn-outline-dark mr-5">Retour</a>
        </div>
    </div>
{% endblock %}
