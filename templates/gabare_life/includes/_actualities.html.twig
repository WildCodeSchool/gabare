<<<<<<< HEAD
<h2 class="text-center m-0 m-md-5">Les actualités de la Gabare</h2>

<div class="row flex-wrap justify-content-center">
    {% for actuality in actualities %}
    <div class="card-center m-2" style="width: 20rem;">
        <img class="card-img-top" src="{{ actuality.image }}" alt="Card image cap">
            <div class="card-body">
            <h3 class="card-title font-weight-bold">{{ actuality.title }}</h3>
            <h4 class="card-title">{{ actuality.theme.name }}</h4>
            <p class="card-text">{{ actuality.description }}</p>
                <p class="card-text">{{ actuality.date | date("d/m/Y") }}</p>
            <a href="#" class="btn btn-primary">Consulter</a>
            </div>
    </div>
    {% endfor %}
</div>


<!--Consulter les autres articles-->

<div class="container">
    <div class="historic text-center m-4">
        <div class="center-block">
            <button class="btn btn-primary center-block btn-lg"><a href="#" class="">Historique</a></button>

<section class="bgActualities pb-5">
    <h2 class="text-center pb-3 pb-md-0">Les actualités de la Gabare</h2>
    <div class="container-fluid">
        <div class="row flex-wrap m-0 justify-content-around">
            {% for actuality in actualities %}
                    <div class="card m-0 mb-5 m-md-5 box col-11 col-md-3" >
                        <img class="card-img-top" src="{{ actuality.image }}" alt="Card image cap">
                        <h5 class="theme position-absolute fixed-top mt-2 pl-2 pr-2 p-1">{{ actuality.theme.name }}</h5>
                        <div class="card-body text-center">
                            <h5 class="card-title">{{ actuality.title }}</h5>
                            <p class="card-text overflow-ellipsis">{{ actuality.description|truncate(80, true) }}</p>
                            <a href="" class="btn btn-primary">Consulter</a>
                        </div>
                    </div>
            {% endfor %}
        </div>
        <div class="historic text-center">
            <a href="#" class="btn btn-primary btn-lg">Voir plus d'actualités</a>
        </div>
    </div>
</section>
